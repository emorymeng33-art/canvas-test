# Chat Canvas Demo 离线预览说明

解压后可直接双击 `index.html` 在浏览器中打开预览。

如果浏览器对本地文件资源加载有限制，也可以在解压目录里启动一个本地静态服务：

```bash
python3 -m http.server 8000
```

然后访问：

```text
http://localhost:8000
```

说明：本包已包含页面运行所需的 HTML、CSS、JS、图片、SVG、视频等静态资源。
